env_set

1. python setuptools
	sudo apt-get install python-setuptools

2. python-pip
	sudo apt-get install python-pip

3. virtualenv
	1)sudo easy_install virtualenv
	2)sudo apt-get install virtualenv

4. Flask
	pip install Flask

5. python-dev & python-setuptools
	sudo apt-get install python-dev python-setuptools

6. Pillow
	pip install pillow
	#image process software python packages.
7. Pyscreenshot
	pip install pyscreenshot
	#linux screen capture python packages.
	
